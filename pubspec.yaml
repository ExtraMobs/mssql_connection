name: sql_server_wrapper
description: SQL Server client using Dart FFI and bundled FreeTDS. Requires verified TLS. See README for platform validation status; web is not supported.
version: 0.0.1
homepage: https://github.com/ExtraMobs/mssql_connection

environment:
  sdk: ^3.9.0
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^6.0.0
  test: ^1.25.6

flutter:
  plugin:
    platforms:
      windows:
        pluginClass: MssqlConnectionPlugin
